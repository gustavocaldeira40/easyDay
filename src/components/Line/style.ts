import styled from 'styled-components';
import { theme } from '../../styles/theme';

export const ContainerOfLine = styled.div`
  border-bottom: 3px solid ${theme.colors.background};
  margin-top: 5px;
  border-radius: 2px;
`;
